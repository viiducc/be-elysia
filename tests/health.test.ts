import { expect, test } from 'bun:test';

// This is a placeholder test. For real tests, use supertest or similar to hit the running server.
test('health check route', () => {
  expect(1).toBe(1);
});
